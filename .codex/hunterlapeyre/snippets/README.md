# Snippets

Keep small, reusable helpers here (scripts, SQL, grep patterns, curl calls).

Guidelines:
- Name them clearly (`what-it-does.*`).
- Include a short header comment: purpose + usage + expected output.
- Prefer non-destructive defaults (read-only, dry-run).

