import { describe } from 'node:test'
import * as moduleUnderTest from '@/lib/portal-profile'

describe('lib/portal-profile', () => {
  void moduleUnderTest.normalizePortalEditableProfile
  void moduleUnderTest.profileFromOrganization
  void moduleUnderTest.splitMultiline
  void moduleUnderTest.splitCsvOrLines

  describe('normalizePortalEditableProfile', () => {})
  describe('profileFromOrganization', () => {})
  describe('splitMultiline', () => {})
  describe('splitCsvOrLines', () => {})
})
