import { describe } from 'node:test'
import * as moduleUnderTest from '@/lib/stripe-onboarding'

describe('lib/stripe-onboarding', () => {
  void moduleUnderTest.provisionLeadBillingForOnboarding

  describe('provisionLeadBillingForOnboarding', () => {})
})
