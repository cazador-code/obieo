import { describe } from 'node:test'
import * as moduleUnderTest from '@/lib/stripe'

describe('lib/stripe', () => {
  void moduleUnderTest.getStripeClient

  describe('getStripeClient', () => {})
})
